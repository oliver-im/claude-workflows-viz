# 260607-0-build-claude-workflows-viz-workflow-meta-to-svg-and-png — Progress

**Cursor:** all units complete (01–05 done) — plan-level review + merge to `main` pending.

**Review approach:** every unit is built on its own branch so it has a real git diff to review (branch/merge mechanics: units 01–02 merged `--no-ff` into `main`; units 03–05 use the worktree + squash flow — see **Git workflow** below). Gate each unit with `/code-review` (this session is rooted at this repo); if it ever mis-roots to planview, fall back to an independent `feature-dev:code-reviewer` subagent pointed here by path. Per planview v0.3.0, each unit also gets a **codex cross-lineage 2nd opinion** (`codex exec -s read-only` over the unit diff, before commit) — a different model family to catch shared blind spots. codex CLI 0.136.0, logged in. Exact command per unit is in each unit md's Review pipeline.

## Pre-execution review

Before starting the first unit, run these against the freshly materialized plan dir:

- [~] `/planview:pre-plan-review` — waived this session: the design was adversarially stress-tested during planning (SVG-vs-Mermaid, single-binary-vs-deps, scope, prior-art, naming). Run on request.

## Git workflow

This plan is worked in its own git **worktree**, one branch per unit (squash-merged), per planview v0.3.0's `git_workflow`. Units 01–02 + the codex `__proto__` fix already landed on `main` directly (pre-worktree); units 03–05 follow the flow below.

- **Worktree:** `worktrees/260607-0-build-claude-workflows-viz-workflow-meta-to-svg-and-png/` checked out on branch `plan/260607-0-build-claude-workflows-viz-workflow-meta-to-svg-and-png` (off `main`). `node_modules` is symlinked to the main checkout's (deps are shared). The `plan/` dir stays at its current path (shared with `main`), not relocated under `active/`.
- **Per unit (03–05):** branch `unit/NN-slug` off the plan branch → build + the two-reviewer gate (`/code-review` + codex) → `git merge --squash unit/NN-slug` into the plan branch as one `Unit NN: <title>` commit → `git branch -D unit/NN-slug` → advance the cursor here.
- **At the end:** after the last unit + plan-level review land on the plan branch, `git checkout main && git merge --no-ff plan/260607-0-build-claude-workflows-viz-workflow-meta-to-svg-and-png`, then `git worktree remove worktrees/260607-0-build-claude-workflows-viz-workflow-meta-to-svg-and-png`.

## Done

- **01-scaffold-the-claude-workflows-viz-project** — new sibling repo `../claude-workflows-viz` scaffolded (TS + esbuild + commander + vitest + zod + acorn + `@resvg/resvg-js`). `npm run build` → `dist/cli.js`; `--version`/`--help` work; `tsc --noEmit` clean; smoke tests pass. CLI action is an intentional stub until Unit 04. **Reviewed:** scaffold clean (esbuild config, shebang/banner, `external`, smoke tests all correct). **Committed:** merge `f92a3a6`. **2nd-cycle review (planview v0.3.0 gate):** clean — Claude + codex cross-lineage both found nothing.
- **02-extract-and-validate-the-workflow-meta-block** — `ts/extract-meta.ts` (acorn parse → locate `meta` init → **static AST literal evaluator**, no execution) + `ts/model.ts` (zod) + 5 fixtures. `tsc --noEmit` clean; 9 tests pass (incl. a regression test proving a getter in `meta` is rejected, not executed). **Reviewed** by independent subagent; 2 findings, both fixed: (1) `node:vm` `runInNewContext` *executed* getters/methods → replaced with the static evaluator to uphold "never execute"; (2) value `import { z }` used only as type → `import type { ZodError }`. **Committed:** merge `facb322`. **2nd-cycle review (planview v0.3.0 gate):** execution-safety invariant re-confirmed by both lineages; codex cross-lineage caught a **Medium** Claude missed — a `{ __proto__: {...} }` meta literal set the built object's prototype so zod validated a malformed meta; **fixed** (null-proto literal + regression test, merge `5ae2c92`). Low finding (bare `const meta` tolerated vs `export const meta`) acknowledged — kept lenient by design (drafts/contract tolerance).
- **03-render-the-phase-flow-svg** — `ts/render-svg.ts` `renderSvg(meta): string`: pure-layout SVG (no engine) — header card (name/description/optional whenToUse) + a vertical stack of phase cards (index chip, single-line title, wrapped detail, model-colored badge adapted from planview's palette + neutral fallback). SVG-safe escaping; char-width fit/truncate/wrap; handles no-phases + no-detail. `tsc` clean; 19 tests (incl. representative snapshot + full-label escaping). **Two-reviewer gate:** Claude clean (flagged a test-coverage gap) + **codex caught the uncapped model badge** (a long `model` could size the badge past the card's left edge into the chip/title) — both folded in: badge label capped at 28 chars, escaping test extended to `whenToUse`/`model`. **Squash-merged** to the plan branch as `6befe68`. Not wired to the CLI (Unit 04).
- **04-rasterize-to-png-and-wire-the-cli-end-to-end** — the must-have image pipeline + end-to-end CLI. `ts/render-png.ts` `svgToPng(svg): Buffer` via `@resvg/resvg-js` (native, no browser; 2× zoom for hi-dpi crispness). `ts/cli.ts` now real (the Unit 01 stub is gone): `extractMeta` → `renderSvg` → png `svgToPng` / html `wrapSvgHtml` / svg → route to `--out`, else stdout for text formats, else a derived default path; `--open` opens the result; `--format` validated and inferred from the `--out` extension. `ts/output.ts` `openBrowser` (adapted from planview) + `ts/html.ts` `wrapSvgHtml` (minimal self-contained page; the optional `--format html`). `tsc` clean; **25 tests** (cli.smoke now drives the built binary: svg file, stdout streaming, PNG magic-bytes + non-zero IHDR dims, format inference, missing-file + bad-format error paths). Manually verified the rasterized PNG renders correctly (header + 3 color-badged phase cards). **Two-reviewer gate:** Claude (code-reviewer subagent) clean; **codex caught a Medium** — Windows `--open` via `cmd /C start` was metacharacter-injectable from a hostile path — **fixed** by opening through `explorer.exe`. **Squash-merged** to the plan branch as `de9d86d`.
- **05-sample-docs-and-install-verification** — "easy install" made real and verified. `examples/review-pr.js` (a realistic 4-phase review workflow: a static `meta` + a never-executed orchestration body using `phase`/`agent`/`pipeline`/`parallel`/`log`) + `examples/review-pr.png` (rendered hero). `README.md` fleshed out: install (`npm i -g` / `npx`), an options table, per-format examples (with a placeholder file + a correct bundled-sample callout), a "how it works" (acorn → static literal eval → zod → SVG → resvg), and from-source. `package.json`: `examples` added to `files` (the sample + hero ship, and the npm README renders the image), and `prepack: npm run build` (the gitignored `dist/` is always rebuilt into the tarball). **Install verified end-to-end:** `npm pack` → install the tarball in a clean project → the bundled sample renders to SVG + PNG via the installed bin (shebang + `bin` symlink + `npx` all work; externalized `@resvg/resvg-js` resolves from a real install). **Two-reviewer gate:** Claude caught a README path that would fail for global installs (fixed); codex clean. **Squash-merged** to the plan branch as `dfd11ee`.

## Blockers

_None._

## Notes

- **Git workflow:** see the **## Git workflow** section above for branch/worktree/merge mechanics. Always verify `tsc --noEmit` + tests green before committing a unit.
- **Design context & rationale** (why SVG-not-Mermaid, TS-not-Rust, scope, prior art, naming, future roadmap): read `../../docs/design-context.md` — captures the decisions behind this plan from the design conversation.
- When resuming, read this file first to find the cursor unit, then read the cursor unit's md. Skip `overview.md` unless this is the first session on the plan.
- On the **first session**, run the Pre-execution review checklist above before starting the cursor unit. Surface findings and revise the plan if anything material lands.
- Work one unit at a time. After finishing the cursor unit, run its review steps, then update this file: move the unit into Done with a one-liner and advance the cursor to the next unit id.
- Stop after each unit. Surface a brief summary to the user and wait for explicit go-ahead before starting the next unit. If the unit is blocked, record it under Blockers and stop without advancing the cursor.

## Plan-level review

After the last unit's review lands and is committed, run these against the cumulative plan diff:

- [x] `/planview:plan-review-prompt` — ran the composer (vehicle: `codex exec -s read-only`, mode exec) over the cumulative diff `29d8c24..HEAD` (base `main`; units 01–02 are below the merge-base on `main`, read as context). Composed a 5-target cross-unit focus: Meta-contract seam (02→03→04), escaping seam (03→04), forward-reference wiring, CLI routing totality, coverage-vs-claims. **codex verdict: No material findings — Ship.** Confirmed `renderSvg` matches the established `Meta` schema, hostile meta strings only reach escaped SVG text, HTML wraps escaped SVG/title safely, CLI routing keeps PNG buffers out of stdout, and the workflow path is only read/parsed (never executed); residual `--format html` / `--open` coverage gaps judged acceptable (`--open` deliberately manual).
- [x] **Claude-lineage plan-level pass** (cross-lineage symmetry, via the `feature-dev:code-reviewer` subagent over the same `29d8c24..HEAD` range + focus — the per-unit Claude reviews were plan-blind to these seams). Seams 1–4 confirmed sound (meta-contract fully handled incl. empty-string optionals; escaping correct in both the SVG and HTML output contexts; all forward-references wired; routing total + never-execute holds end-to-end). **One MED:** `wrapSvgHtml` / `--format html` is a delivered, README-documented surface with no automated coverage → **fixed** by adding `ts/__tests__/html.test.ts` (verbatim-inline + title-escaping). 27 tests green. Verdict after fix: **Ship.**
