# Unit 07 — CLI default flip, end-to-end smoke, example regen, and docs
**Blocked by:** 06-banded-layout-engine-topology-renderer**Agents involved:** main only**Topology:** none
## Summary

Wire the pipeline into the CLI with topology as the default view, regenerate all committed renders, and update docs. This closes every forward reference (Units 02–06 modules all become live).

### Deliverables

1. **`ts/cli.ts`**: read the file once (`readWorkflowSource`) → `parseWorkflowSource` → `extractMetaFromProgram` → analyzer+flattener inside try/catch (any throw → one-line stderr warning + `EMPTY_IR`, i.e. v1-equivalent output, exit 0) → `--view <view>` option, `topology` (default) | `phases` (forces `renderSvg`); unknown value → existing `fail()` path. stdout/file/`--open`/PNG/HTML routing untouched.
2. **Smoke tests** (`cli.smoke.test.ts` additions): default on `examples/summarize-codebase.js` → output contains `agent-node` + `barrier`; `--view phases` → no `agent-node`, byte-stable v1; bad `--view` → exit 1 + message; `--help` lists `--view`; PNG via topology path still magic-byte-valid.
3. **Regenerate** all `examples/*.svg` through the default (topology) view; regenerate `examples/review-pr.png` (hero: pipeline ×3 named lanes + ×N verify + barrier). Eyeball each PNG/SVG render before committing (Read the PNGs).
4. **README.md**: hero swap; `--view` row in the options table; "Examples" gallery section — a table linking all 8 workflows to their committed SVGs with one-line pattern names (classify-and-act, fanout-and-synthesize, adversarial verification, generate-and-filter, tournament, loop-until-done, dual-lineage, review-pr); "How it works" gains one paragraph: the body is statically analyzed (never executed) and unrecoverable structure degrades honestly (×N, opaque steps, v1 cards).
5. **`docs/design-context.md`**: §7 roadmap — mark body-AST topology landed (v2), note the constraint-first hand-rolled layout choice (dagre/elk still the upgrade path); §8 updated: the Six-Patterns look is now the default for recognized idioms.
6. `npm pack --dry-run` sanity: dist + README + examples (js/svg/png) all shipped.

### Acceptance

Full suite green; all 8 examples render in both views without warnings; the analyzer-failure fallback proven by a smoke fixture (a meta-only file with an exotic body still exits 0 and renders v1-style); README images resolve to committed paths.

Review focus: the default flip's blast radius (every code path that previously produced v1 SVG), the try/catch fallback honesty (warning visible, never silent), docs accuracy against actual behavior.

## Verification (plan-wide)

1. `npm test` green at every unit boundary; the v1 `render-svg` snapshot stays byte-identical from Unit 02 onward (`--view phases` is the permanent regression surface).
2. After Unit 07: `node dist/cli.js examples/<each>.js -o /tmp/x.svg` (and `--format png`) for all 8 — zero stderr warnings, then visually inspect the PNGs (Read tool) — expect: fan-out circles + barrier (summarize, verify-fix, name-the-feature), named expanded labels (verify-fix, choose-approach), pipeline lanes crossing bands (review-pr), gutter loop arcs (hunt-bugs, choose-approach), decision diamonds with yes/no (triage, hunt-bugs), two-band parallel branches (dual-lineage).
3. `node dist/cli.js examples/triage-issue.js --view phases | diff - <(git show <pre-plan>:examples/triage-issue.svg)` — confirms v1 preserved end-to-end. (Any of the seven committed v1 SVGs works here; review-pr has no committed SVG — only the PNG hero.)
4. `npm pack --dry-run` lists examples; `npx . examples/triage-issue.js --open` spot check.

## Review pipeline

- [x] `/code-review` — done 2026-06-13: 2 focused lanes. Lane 1 (CLI flip blast radius + fallback honesty, probe-executed incl. fault injection on a /tmp copy of the bundle): clean on blast radius (routing block character-identical to HEAD^; default→stdout, `-o`, `--format png`, ext-inferred png, unrouted-PNG→cwd, html `<title>`, and `--view phases` ≡ HEAD^-committed v1 SVG end-to-end all probed in both views), error-path identity (the read→parse→extract composition is definitionally the old `extractMeta`; identical messages + exit 1), resolveView, no partial-assignment hazard in the fallback, totality probes (100k-deep literal dies in acorn parse exactly as before; 200k member chain renders fine; could not make the analyzer throw from real file input), fixture soundness, strictness/build. 3 findings: (1) the throw→warning catch has zero automated coverage — a regression making it fatal or silent would ship undetected (conf 90) → **accepted + documented**: the belt is defensive-only for a contractually-total analyzer (own totality test group); the reachable degradation (no orchestration → v1 page) is pinned byte-for-byte end-to-end; pinning the catch would need bundle-text injection (brittle) or a DI seam (indirection the 30-line CLI deliberately avoids); current behavior verified by BOTH lane 1 and codex via fault injection (analyzeBody-throw and flattenTopology-throw → one-line warning, exit 0, stdout byte-identical v1); (2) warning text degrades for non-Error/multi-line throws — `(e as Error).message` prints "undefined" for `throw "stringy"`, newline-bearing messages break the one-line contract (conf 95) → **fixed in-unit**: reason derived via `e instanceof Error ? e.message : String(e)` + whitespace-collapse; (3) the EMPTY_IR byte-identity pin didn't cover a zero-phases meta — `metaSchema` admits `phases: []` and the CLI reaches `renderTopologySvg(meta, EMPTY_IR, [])`, probed byte-identical but unpinned (conf 90) → **fixed in-unit**: phase-less byte-identity test added. Lane 2 (docs accuracy + artifact provenance): clean on provenance (all 7 SVGs byte-identical to fresh default-view output of a rebuilt dist; review-pr.png byte-identical; zero stderr), v1 preservation (all 7 `--view phases` ≡ `git show main:examples/*.svg`), options table ≡ `--help`, gallery links + per-row pattern claims vs actual bodies, "How it works" step 4 claim-by-claim, Status/deps, design-context §7/§8/§9 (plan id, module names, unit counts all verified), npm pack (19 files, no link 404s in-tarball), hero genuinely swapped. 2 findings: (1) README says "v2" while package.json stays 0.1.0 — a published artifact would change default output with no version signal (conf 55) → **accepted + surfaced to the user**: "v2" is the design-doc generation label used since design-context was written; the plan never scoped a version bump and the package is unpublished — bumping is a release decision for the user; (2) "Agent circles inside the graph are colored the same way" is true (same `swatchFor`, model read from `agent()` opts) but no bundled example passes `model`, so the gallery never demonstrates it (conf 35) → **accepted**: it documents behavior, like the options table; verified true by the lane itself.
- [x] `codex exec -s read-only 'Second opinion on the diff HEAD^..HEAD … this is the final unit, so NO deferred forward-references should remain — flag any module/contract from units 02-06 that is still unwired.'` — **exec**: done 2026-06-13: "No local correctness issue found in the CLI default flip itself"; independently verified the default, `--view phases` v1-style output, bad `--view` exit 1, and all committed example SVGs matching current CLI output; no unwired module/contract from units 02–06 spotted. 2 Lows: (1) `ts/model.ts` doc comment still said the imperative body "is never read or executed" — stale after the flip (the only stale v1 wording outside historical plan docs) → **fixed in-unit**: comment now says the topology view analyzes the body statically, read off the AST, never executed; (2) the exotic-body smoke test proves the no-orchestration fallback, not the catch path — same gap as lane 1's finding 1 → **accepted + documented** (same disposition; fault-injection verification on record from both reviewers).

_Template steps are recorded verbatim; the **resuming agent** substitutes their placeholders per the resume protocol before running — the renderer never substitutes._
---
See `progress.md` for the cursor and overall plan state.
